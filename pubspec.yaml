name: native_push_client
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.0-282.3.beta <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  json_annotation: ^4.9.0
  native_push:
    path: ../native_push

dev_dependencies:
  build_runner: ^2.4.9
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.8.0

flutter: